{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Everything the Snot window is allowed to ask the backend for.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
